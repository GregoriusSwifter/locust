#ifndef TYPES_H
#include <locust/type.types.h>
#endif

// Negative values denote C types that are not Locust types.
// C (not Locust) types
META_DEF(size_t);
META_DEF(uintptr_t);
META_DEF(ptrdiff_t);
